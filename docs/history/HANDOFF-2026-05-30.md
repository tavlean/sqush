# Handoff — 2026-05-30 (UI colleague pass)

> **Branch-name note (updated 2026-05-31):** the branches this doc calls
> `code/sveltekit-single-image-slice` and `code/sveltekit-codec-assets` were
> later collapsed into a single trunk named **`svelte`** (worktree
> `../Presk-svelte`). The narrative below is preserved as a point-in-time record;
> for current state read [the project brief](../project/brief.md) and [MIGRATION-PLAN.md](MIGRATION-PLAN.md).

Short version: I came in to make the project easy to resume — audited the
branches, consolidated the picture, cleaned the docs, and built the first real
UI (the single-image compressor) over the engine you already proved. Backend
contracts were left untouched.

## What I changed

All work is on a new branch **`code/sveltekit-single-image-slice`**, cut from
`code/sveltekit-codec-assets` (the canonical branch). Nothing on `main` or the
existing branches was modified.

1. **Runnable single-image compressor** (the UI you left for me):
   - `prototypes/sveltekit/src/lib/compress.ts` — adapter: browser `File` + UI
     settings → decode/preprocess/resize via the shared image-pipeline helpers,
     then encode through the SvelteKit codec worker bridge.
   - `prototypes/sveltekit/src/routes/+page.svelte` — drop/pick a file, choose
     WebP/AVIF/JXL, quality slider, optional resize; live preview + size + %
     saved + download. Svelte 5 runes, debounced re-encode with abort.
   - `prototypes/sveltekit/src/routes/diagnostics/+page.svelte` — the previous
     probe page, moved verbatim (nothing lost). Linked from the home page.
2. **Docs**: added `docs/STATUS.md` (single entry point) and
   `docs/dashboard.html` (clear status board). Retired the confusing
   `docs/progress-dashboard.{html,md}`. Archived point-in-time docs to
   `docs/history/` (`todo.md`, `upstream-pr-notes.md`).

## One thing worth knowing (encode path)

I first wired the compressor through `processBulkImageJob` →
`imagePipeline.compressImage`, but the prototype's generated `encoderMap` /
features-worker is **WebP-only** — AVIF/JXL are proven via direct bridge calls
(`bridge.avifEncode` / `bridge.jxlEncode`), not through `compressImage`. So the
helper encodes directly through the bridge per format. If you want the
single-image and bulk paths to share `compressImage` for all three formats,
widen the generated active encoder surface (sync generator + active set) to
include avif/jxl, then the helper can switch back to `processBulkImageJob`.

## Branch consolidation (recommendation, not executed remotely)

`code/sveltekit-codec-assets` fully contains `code/sveltekit-migration-seams`
and `code/sveltekit-prototype` (0 commits of either are missing from it). So:

- Keep: `main`, `code/sveltekit-codec-assets`, and this slice branch.
- Retire when ready: `migration-seams` and `prototype` (redundant subsets).
- I did **not** delete or push any remote branches.

## How to run / verify

```bash
cd prototypes/sveltekit
npm install          # ensures root + prototype deps (the offline worktree I
                     # used could not install; see note below)
npm run dev          # open the printed localhost URL, drop an image
npm run check        # svelte-check
npm run build        # vite build + static-output audit
```

## Verification status (read this)

`npm run check` in my worktree reported **one remaining error**:
`Cannot find module 'comlink'` in
`src/client/lazy-app/worker-bridge/runtime.ts`. That is **environmental**, not a
code problem — the isolated worktree's root `node_modules` was never installed
(no network), so tsc can't resolve `comlink` for the production files. Vite
resolves it from the prototype deps at dev/build time, and a normal root
`npm install` makes svelte-check clean. After a fresh install, expect 0 errors.

My own files are clean (the earlier `compress.ts` type error — routing AVIF/JXL
through the WebP-only `encoderMap` — is fixed in the committed version). Browser
smoke (drop a real JPEG/PNG → confirm WebP/AVIF/JXL output + download) is the
remaining manual check; I could not run a browser in this environment.

## Good next steps

1. Browser-verify the slice; tune default quality per format.
2. Build the bulk UI on the same helpers (import strip → per-image overrides).
3. Decide the build tool (keep Rollup for Preact vs. go Vite/SvelteKit).
4. Optionally widen the generated encoder surface so all formats share
   `compressImage`, then unify single-image and bulk on `processBulkImageJob`.
