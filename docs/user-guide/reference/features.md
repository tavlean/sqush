# Frisp editor — feature reference

Exhaustive, code-derived inventory of every feature and user interaction in the
editor and bulk mode. Each entry lists the source file and one precise sentence
on what it does. Derived purely from reading the source — see "Notable / hidden
behaviors" at the end for things not surfaced in the obvious UI.

## Loading & navigation

| Feature                                | File                                                     | What it does                                                                                                                                                                                                       |
| -------------------------------------- | -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Intro landing screen                   | `src/lib/editor/intro/Intro.svelte`                      | Shown while no file is loaded (the `{:else}` branch of `+page.svelte` renders `<Intro>`): a full-viewport dashed viewfinder frame with the chrome reduced to HUD micro-copy — the brand lockup top-left and the format list (WebP, SVG, AVIF, JPEG XL, PNG, JPEG) as a spaced `<ul>` centered along the bottom; the centre column holds a tray icon, the headline "Drop images to optimize." (swaps to "Release to add." while dragging, "Add images to optimize." on a coarse-pointer/touch device), a subline ("Free, open-source image compression that runs offline in your browser. Nothing uploads."), a "Browse files" button, and a quiet "or paste" action beside it. Themes off the OS via CSS `light-dark()`, no manual toggle. |
| Browse / paste actions                 | `src/lib/editor/intro/Intro.svelte`                      | A "Browse files" `<button>` clicks a hidden multi-file `<input type="file" accept="image/*" multiple>` whose `onchange` runs `onPick`, sending the files up through `routeFiles` (2+ supported images open bulk, 1 opens the single editor). A quiet "paste" ghost button (async clipboard read, shown when supported) and window ⌘/Ctrl+V paste add clipboard images the same way. Folders import by drag-and-drop (below), not a picker.                                                                                          |
| Drag-and-drop to load/replace          | `src/lib/editor/file-drop.ts`, `src/lib/editor/intro/Intro.svelte`, `src/routes/+page.svelte` | Dropping images anywhere loads or replaces the current file instead of letting the browser open them: `.app-root` carries the `fileDrop` attachment for the open editor, while the intro attaches its own drop target (with `stopPropagation`) so a landing drop routes exactly once. Both paths land in `routeFiles`, and dropped folders are walked recursively.    |
| Drag-over visual feedback              | `src/lib/editor/file-drop.ts`, `src/lib/editor/intro/Intro.svelte`, `src/routes/+page.svelte` | Over the open editor, a file-bearing drag toggles a `drop-valid` class on `.app-root`, fading in a coral dashed full-viewport overlay (an enter/leave depth counter prevents flicker over nested children). Over the intro the coral overlay never shows: the intro stops propagation and instead lights its own dashed viewfinder frame (accent stroke plus inner glow) as the drag feedback.                    |
| Files-only drag filter                 | `src/lib/editor/file-drop.ts`                            | `dragHasFiles` ignores drags that carry only text/links (no `Files` type), so text drags don't trigger the overlay or `preventDefault`.                                                                            |
| Non-image rejection                    | `src/routes/+page.svelte` (`routeFiles`), `src/client/lazy-app/bulk/import.ts` (`isSupportedBulkImage`) | Dropped/picked files are filtered by `isSupportedBulkImage` (by extension); if none qualify, `routeFiles` shows the snackbar `No supported images found.` and loads nothing. A secondary MIME guard in `pickFiles` (`editor-session.svelte.ts`) still exists — `"<name>" doesn't look like an image.` — for a file that clears the extension filter but isn't an image. |
| Open pushes history state              | `src/routes/+page.svelte`, `editor-session.svelte.ts`    | Opening the first image calls `pushState('', { editor: true })` so the browser Back button (or gesture) returns to the intro screen.                                                                               |
| Route-state sync / Back closing editor | `editor-session.svelte.ts` (`syncRouteState`)            | An `$effect` watches `page.state.editor`; when it becomes false (Back navigation) while a file is loaded, `clearFile()` is called, returning to the intro.                                                         |
| Back button                            | `src/routes/+page.svelte` (`back`)                       | The round glass "X" button top-left calls `history.back()` (falling back to `clearFile()` if no history), unwinding the pushed editor state.                                                                       |
| Undo / Redo buttons                    | `src/routes/+page.svelte`, `editor-session.svelte.ts` (`undo`/`redo`) | Two glass buttons right of Back step the editor document backward/forward through its history; each is `disabled` via `history.canUndo` / `history.canRedo`. Tooltips show ⌘ vs Ctrl by platform.    |
| Undo / Redo keyboard shortcuts         | `src/routes/+page.svelte` (`onKeydown` on `<svelte:window>`) | `⌘/Ctrl+Z` undoes, `⇧⌘Z` / `Ctrl+Shift+Z` / `Ctrl+Y` redoes. Suppressed when a typeable field (text/number input, textarea, contenteditable) is focused so native text-undo still works; range/checkbox inputs fall through to editor undo. |
| Document title                         | `editor-session.svelte.ts` (`docTitle`)                  | The tab title is `"<filename> - Frisp - Compress images"` (`APP_NAME` = "Frisp"), prefixed with an ⏳ hourglass while either side is encoding.                                                                     |
| Dev-only diagnostics link              | `src/routes/+page.svelte`                                | In dev builds only, a "Pipeline diagnostics →" link to `/diagnostics` appears on the intro screen.                                                                                                                 |

## Bulk optimization

| Feature                              | File                                                                 | What it does                                                                                                                                                                                                 |
| ------------------------------------ | -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Bulk routing threshold               | `src/routes/+page.svelte`                                            | Picking/dropping 2+ supported images opens production bulk mode; picking/dropping 1 supported image keeps the normal single-image editor path.                                                               |
| Append while bulk is active          | `src/routes/+page.svelte`, `src/lib/bulk/store.svelte.ts`            | Additional supported files dropped or picked while a batch is open are appended to the existing bulk session instead of replacing it.                                                                         |
| Folder import (drag only)            | `src/lib/editor/file-drop.ts`, `src/lib/bulk/import-sources.ts`      | Folders import by being dropped onto Frisp (there is no folder picker button): dropped directories are walked recursively via browser directory-entry APIs, reading every batch from `readEntries()` so folders over 100 entries are handled; dot-files and dot-directories are skipped.      |
| Production bulk store                | `src/lib/bulk/store.svelte.ts`, `src/client/lazy-app/bulk/*`         | A Svelte 5 store wraps the framework-neutral bulk engine, owns thumbnails/object URLs, derives strip/summary/detail view models, and drives the production batch UI.                                           |
| Bulk processing runtime              | `src/lib/bulk/runtime.ts`                                            | Runs queued bulk jobs through persistent worker bridges with bounded concurrency, remembering outputs in the store and returning active jobs to the queue when cancelled.                                      |
| Stack resting stage                  | `src/lib/bulk/StackStage.svelte`, `src/lib/bulk/FocusView.svelte`    | When no single image is selected, the stage shows a stack of batch images; selecting one image switches to the focused two-up view.                                                                            |
| Rich strip and strip sizes           | `src/lib/bulk/RichStrip.svelte`, `src/lib/bulk/ViewModePicker.svelte` | Shows the batch thumbnails in S/M/L strip sizes; size changes affect only the viewing density.                                                                                                                 |
| Multi-select                         | `src/lib/bulk/strip-selection.ts`, `src/lib/bulk/store.svelte.ts`    | Click selects one image, Cmd/Ctrl-click toggles, Shift-click ranges, and desktop drag-select sweeps a range; selecting every image routes edits to the global settings.                                       |
| Per-image override dots              | `src/lib/bulk/store.svelte.ts`, `src/lib/bulk/StripCell.svelte`      | `getSettingsOverridePaths` and per-leaf WebP comparisons drive the custom-setting dot on thumbnails and controls.                                                                                            |
| Divider follows image orientation    | `src/lib/bulk/FocusView.svelte`, `src/lib/editor/output/Output.svelte` | Bulk passes `orientationOverride` so landscape/square images keep a left-right divider even on narrow screens, while clearly portrait images use top-bottom; single-image mode keeps the default viewport rule. |
| Save all as ZIP                      | `src/lib/bulk/store.svelte.ts`, `src/lib/bulk/zip.ts`                | `saveAll()` creates a bulk export plan, builds a real ZIP with `client-zip`, triggers a `.zip` download, and marks exported entries as exported.                                                              |
| Keep originals when larger           | `src/lib/bulk/store.svelte.ts`, `src/client/lazy-app/bulk/export.ts` | The `keepOriginalWhenLarger` toggle defaults on; export entries whose optimized file would be larger use the original source file in the ZIP.                                                                 |
| Remove from batch + Undo             | `src/lib/bulk/store.svelte.ts`, `src/lib/editor/snackbar-store.svelte.ts` | Removing images offers snackbar Undo; object URLs for removed jobs are revoked only after the snackbar settles without Undo.                                                                                  |

## Two-up before/after compare (Output)

| Feature                           | File                                                                           | What it does                                                                                                                                                                                                                                         |
| --------------------------------- | ------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Two-up split view                 | `src/lib/editor/output/Output.svelte`, `output/two-up.ts`, `output/two-up.css` | A `<two-up>` custom element overlays the left side (canvas 0, "before") and right side (canvas 1, "after"), clipping each to one side of a movable split point.                                                                                      |
| Left = before / right = after     | `Output.svelte`, `src/routes/+page.svelte`                                     | Left canvas draws `session.results[0].outputImageData` (side 0's output, accented coral), right draws side 1's output (accented azure); both default to side 0=Original, side 1=WebP.                                                                |
| Draggable split handle            | `output/two-up.ts`, `output/two-up.css`                                        | A `.two-up-handle` with a circular glass scrubber (coral ◀ / azure ▶ arrows) is dragged via `PointerTracker`, clamped to `[0, bounds]`, updating the `--split-point` CSS var that drives both clip-paths.                                            |
| Relative split position retained  | `output/two-up.ts` (`_resetPosition`)                                          | The handle's position is stored as a fraction (`_relativePosition`, default 0.5) and re-derived on every resize via a `ResizeObserver`, so the split stays proportional when the window changes.                                                     |
| Orientation (horizontal/vertical) | `Output.svelte` (`orientation` derived), `two-up.css`                          | The split is vertical (top/bottom) when the viewport is ≤760px wide and horizontal (left/right) otherwise; the handle, clip-paths, and scrubber rotation all switch accordingly.                                                                     |
| Keyboard split presets (1/2/3)    | `output/two-up.ts` (`_onKeyDown`)                                              | Pressing `1`/`2`/`3` (Digit or Numpad) snaps the split to start, middle, or end — but is ignored while focus is inside an `<input>`.                                                                                                                 |
| Synced pan/zoom across sides      | `Output.svelte` (`onLeftChange`), `output/pinch-zoom.ts`                       | Only the left `<pinch-zoom>` is driven; its `change` event mirrors scale/x/y onto the right `<pinch-zoom>` so both halves stay perfectly aligned.                                                                                                    |
| Event retargeting to left side    | `Output.svelte` ($effect with capture listeners)                               | Touch/mouse/wheel (+ pointerdown on non-Safari) events on the two-up are cloned and re-dispatched to the left pinch-zoom; drags over the handle pass through (so the slider works) but wheel always retargets so zooming works even over the handle. |
| Pinch-zoom (touch)                | `output/pinch-zoom.ts` (`_onPointerMove`)                                      | Two-finger pinch scales about the gesture midpoint and pans by the midpoint delta; one-finger drag pans the image.                                                                                                                                   |
| Auto fit + centre on load/resize  | `Output.svelte` ($effect with `fittedKey`)                                     | On a new file, dimension change, orientation flip, or viewport resize (keyed, so re-encodes at the same size don't refit), the image is scaled to fit (never above 1×) and centred inside the inset-adjusted viewport via a `requestAnimationFrame`. |
| Fit insets                        | `Output.svelte`, `src/routes/+page.svelte` (`--fit-inset-*`)                   | Desktop reserves 300px on each side (`--fit-inset-left/right`) so the fitted image clears the two option panels; on mobile (≤760px) these insets collapse to 0.                                                                                      |
| Contain-letterbox alignment       | `Output.svelte` (`containStyle`), `editor-session.svelte.ts` (`sideContains`)  | When a side uses Resize with fit-method "contain", its canvas CSS box is pinned to the original source dimensions with `object-fit:contain`, so a smaller output stays letterboxed inside the original footprint and the two halves remain aligned.  |
| Vector-true SVG preview           | `src/routes/+page.svelte`, `output/SvgPreview.svelte`                          | When a side shows the optimized `svg` output, an `<img>` overlay (`data-pinch-overlay`) re-rasterises the SVG at every zoom level — its layout width/height and `translate` mirror the driven pinch-zoom transform instead of sitting under it — so the vector stays crisp at any magnification; past 8192px on the long edge the residual zoom is applied as a bitmap `scale`. |

## Output controls (zoom / rotate / view options)

| Feature                      | File                                                   | What it does                                                                                                                                                                                                                |
| ---------------------------- | ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Zoom out button              | `Output.svelte`                                        | Multiplies the current scale by `1/1.25` about the container centre (50%/50% origin).                                                                                                                                       |
| Zoom in button               | `Output.svelte`                                        | Multiplies the current scale by `1.25` about the container centre.                                                                                                                                                          |
| Zoom percentage display      | `Output.svelte` (`scalePercent`)                       | Shows the current scale rounded to a whole percent (`Math.round(scale*100)`), with a dashed underline cueing it's editable.                                                                                                 |
| Editable zoom % field        | `Output.svelte` (`editingScale`, `onScaleInput`)       | Clicking/focusing the percent (or pressing Enter on it) swaps it for a `number` input (1–1,000,000, step 1); typing a value zooms to `percent/100`, blur restores the static display.                                       |
| Wheel / trackpad zoom        | `output/pinch-zoom.ts` (`_onWheel`)                    | Mouse-wheel and trackpad pinch zoom about the cursor; `ctrlKey` (trackpad pinch) uses a tighter divisor (100 vs 300), and "lines" delta mode is scaled ×15.                                                                 |
| Scale clamping               | `output/pinch-zoom.ts` (`MIN_SCALE`/`MAX_SCALE`)       | Transform updates below 0.01× or above 100000× are rejected.                                                                                                                                                                |
| Pan bounds                   | `output/pinch-zoom.ts` (`setTransform`)                | The positioned content can't be panned fully off-screen — x/y are corrected so it always overlaps the container.                                                                                                            |
| Rotate button                | `Output.svelte`, `editor-session.svelte.ts` (`rotate`) | Advances the shared preprocessor rotation by +90° (mod 360), re-encoding both sides at the new orientation.                                                                                                                 |
| Rotate swaps resize dims     | `editor-session.svelte.ts` (`rotate`)                  | On an orientation-changing rotate (90/270), each side's resize width/height are swapped so the Resize fields and preset stay aligned with the rotated source — done for both sides regardless of whether resize is enabled. |
| Rotate hidden for SVG        | `src/routes/+page.svelte`, `Output.svelte` (`onRotate`) | For a vector (SVG) source `+page.svelte` passes `onRotate={undefined}`, so `Output.svelte` (`{#if onRotate}`) never renders the Rotate button — the optimize lane keeps the source vector, so there is no rasterized orientation to rotate. |
| View options popover          | `Output.svelte` (`viewOptionsOpen`, `viewOptionsDirty`) | A "View options" trigger (sliders icon, next to Rotate) opens a popover above the bar holding the two preview-only toggles below. Light-dismiss via outside-`pointerdown` (capture) + Escape (Escape restores focus to the trigger); `aria-haspopup`/`aria-expanded` on the trigger. A dirty dot shows on the trigger whenever either toggle is non-default. |
| Smoothing toggle (pixelated)  | `Output.svelte` (`pixelated`)                          | A toggle **row inside View options** (non-Safari only) switches both canvases between smooth and `image-rendering: pixelated` so you can inspect pixels when zoomed in; the row shows On/Pixelated and reflects state via `aria-pressed`.                                            |
| Background toggle             | `Output.svelte` (`altBackground`)                      | A toggle **row inside View options** that turns the dark 80%-opacity backdrop behind the image on/off (fades over 500ms) so you can judge the result against a light vs dark background; the row shows Dark/Light and reflects state via `aria-pressed`.                                              |

## Per-side Edit / Compress panel (OptionsPanel)

| Feature                          | File                                                    | What it does                                                                                                                                                                                                                                                                                                                                                                                                   |
| -------------------------------- | ------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Two bottom-corner panels         | `src/routes/+page.svelte`, `ImageInfoPanel.svelte`, `OptionsPanel.svelte` | Each side gets a 300px content-height card pinned to a bottom corner; the left card is contextual image info while side 0 is Original, then becomes an encoder panel when Compare as… selects a real format, while the right card remains the normal encoder panel.                                                                                                                                              |
| Left image-info panel            | `ImageInfoPanel.svelte`, `ImageInfoRows.svelte`         | When `sides[0].format === 'identity'`, the left column shows filename plus shared image-info rows: original format, original size, dimensions, and inferred aspect chip.                                                                                                                                                                                                                                      |
| Compare as… popover              | `ImageInfoPanel.svelte`, `compress.ts` (`OUTPUT_FORMATS`) | The left info panel's **Compare as…** button opens a five-format menu (WebP, AVIF, JPEG XL, JPEG, PNG); choosing one calls `session.setFormat(0, format)` and swaps the left column into the encoder `OptionsPanel`.                                                                                                                                                                                           |
| Panel layout order               | `OptionsPanel.svelte`                                   | Top-to-bottom: an "Edit" section (Resize + Reduce-palette, hidden on the Original side), a "Compress" section (encoder picker + that encoder's options), and a pinned results footer (size, size-change badge, download). The left panel only uses this layout while compare mode is active.                                                                                                                   |
| Encoder/format picker            | `OptionsPanel.svelte`, `compress.ts` (`OUTPUT_FORMATS`) | A large `Select` lists "Original Image" plus every available format by plain name (WebP, AVIF, JPEG XL, JPEG, PNG); the underlying encoder is a hover tooltip (title) on each entry and the collapsed control. Choosing one calls `session.setFormat`; on the left side, choosing Original Image returns to the info panel.                                                                                     |
| Close compare button             | `OptionsPanel.svelte`                                   | While the left side is in compare mode, a ✕ button in the Edit header calls `onCloseCompare`, setting side 0 back to Original/ImageInfo without adding separate panel-mode state.                                                                                                                                                                                                                              |
| "Original Image" pseudo-encoder  | `compress.ts` (`IDENTITY`), `OptionsPanel.svelte`, `ImageInfoPanel.svelte` | The identity option shows the preprocessed source unchanged and downloads the original file as-is; on the left side it is represented by the image-info panel by default, while the right side still renders it as an option panel if selected.                                                                                                                                                                  |
| SVG optimize lane (vector sources) | `editor-session.svelte.ts` (`availableFormats`/`pickFiles`), `OptionsPanel.svelte` | For an SVG source the encoder picker prepends an **SVG** entry (id `svg`, "SVGO" hover tooltip) and side 1 (the "after") defaults to it; choosing it runs the vector optimize lane (SVGO) instead of rasterising to a WASM codec. While the `svg` format is active the whole "Edit" block (Resize / Film grain / Reduce palette) is hidden (`format !== 'svg'`), since the raster processors don't apply. The left "Compare as…" menu still lists only the five raster formats. |
| Resize section toggle            | `OptionsPanel.svelte`, `ResizeOptions.svelte`           | A "Resize" `Toggle` enables/disables the resize processor; when on, a slide-in panel of resize controls appears.                                                                                                                                                                                                                                                                                               |
| Resize method                    | `ResizeOptions.svelte`                                  | A Select for resampling method: Lanczos3, Mitchell, hqx (pixel art), Browser pixelated — plus a "Vector" option that only appears for SVG sources. (Catmull-Rom and Triangle remain valid worker methods in the code path — `catrom` finishes an hqx pass — but are no longer offered in the dropdown; the browser low/medium/high quality levels were removed.) Lives under the **Advanced settings** reveal. |
| Resize preset                    | `ResizeOptions.svelte`                                  | A Select of percentage presets (from `sizePresets`) plus "Custom"; picking a percent sets width/height from the source dims, and matching dims auto-select the preset.                                                                                                                                                                                                                                         |
| Resize width / height            | `ResizeOptions.svelte`                                  | Number inputs (min 1) for output width and height.                                                                                                                                                                                                                                                                                                                                                             |
| Maintain aspect ratio            | `ResizeOptions.svelte` (`maintainAspect`, default on)   | When on, editing width recomputes height (and vice-versa) from the source aspect ratio.                                                                                                                                                                                                                                                                                                                        |
| Fit method                       | `ResizeOptions.svelte`                                  | When aspect-ratio lock is off, a Select for "Stretch" vs "Contain" appears (Contain drives the letterbox alignment in Output).                                                                                                                                                                                                                                                                                 |
| Premultiply alpha / Linear RGB   | `ResizeOptions.svelte`                                  | Two checkboxes that appear only for the worker-based resize methods; the selectable ones are mitchell/lanczos3/hqx (catrom/triangle stay in the worker set in code but aren't in the dropdown). Under the **Advanced settings** reveal, alongside Method.                                                                                                                                                      |
| Film-grain section toggle        | `OptionsPanel.svelte`, `GrainOptions.svelte`            | A "Film grain" `Toggle` enables the grain processor (runs after resize, before quantize); when on, a slide-in panel appears.                                                                                                                                                                                                                                                                                   |
| Grain Amount slider              | `GrainOptions.svelte`                                   | Range 0–100 (default 12) setting grain strength; the grain character (monochrome, midtone-weighted, fixed seed) is pinned — calibration in `docs/project/specs/2026-07-12-film-grain.md`. Dragging shows a live pre-encode preview (`EditorSession.updateGrainPreview`).                                                                                                                                                |
| Grain size slider (Advanced)     | `GrainOptions.svelte`                                   | Range 1–100 (default 20; 20 units = 1px, ≤20 = finest) particle scale; >20 = band-limited lattice grain, the byte-efficient debanding shape (40 ≈ 2px).                                                                                                                                                                                                                   |
| Reduce-palette section toggle    | `OptionsPanel.svelte`, `QuantizeOptions.svelte`         | A "Reduce palette" `Toggle` enables the quantize processor; when on, a slide-in panel appears.                                                                                                                                                                                                                                                                                                                 |
| Colors slider                    | `QuantizeOptions.svelte`                                | Range 2–256 setting the quantized palette's max color count.                                                                                                                                                                                                                                                                                                                                                   |
| Dithering slider                 | `QuantizeOptions.svelte`                                | Range 0–1 (step 0.01) setting dither amount.                                                                                                                                                                                                                                                                                                                                                                   |
| "Compress" section header        | `OptionsPanel.svelte`                                   | A sticky uppercase section label with a per-side accent tick (coral/azure).                                                                                                                                                                                                                                                                                                                                    |
| Re-key on options identity       | `OptionsPanel.svelte` (`{#key options}`)                | The encoder sub-panel remounts when the options object identity changes (copy/import) so panels that seed UI state once (AVIF/JXL) re-derive; in-place slider edits keep identity and don't disrupt dragging.                                                                                                                                                                                                  |
| Encoder-with-no-options fallback | `OptionsPanel.svelte`                                   | For an encoder with no dedicated panel and no numeric `quality` (no shipped format hits this today — all five have panels), shows "<label> has no adjustable options."; an unknown encoder exposing numeric `quality` gets a generic 0–100 Quality range.                                                                                                                                                      |

## Per-side title-bar actions (Edit header)

| Feature                     | File                                                                             | What it does                                                                                                                                                                                                                                   |
| --------------------------- | -------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| Copy settings to other side | `OptionsPanel.svelte`, `editor-session.svelte.ts` (`copyToOther`)                | An arrow button (pointing toward the other side) copies this side's full settings snapshot onto the other side and shows a "Settings copied across" snackbar with an Undo action (5s).                                                         |
| Save side settings          | `OptionsPanel.svelte`, `editor-session.svelte.ts` (`saveSide`)                   | A gear button persists this side's format + per-format options + processor state to localStorage (`app:side-settings:left                                                                                                                    | right`, versioned), confirming with a "<Left/Right> side settings saved" snackbar (or an error snackbar on failure). |
| Import saved side settings  | `OptionsPanel.svelte`, `editor-session.svelte.ts` (`importSide`)                 | A gear-with-arrow button loads the saved side settings, applying format/options/processor state, with a "<Left/Right> side settings imported" snackbar offering Undo (5s); shows an "invalid" snackbar if the stored payload fails validation. |
| Import disabled until saved | `OptionsPanel.svelte` (`canImport`), `editor-session.svelte.ts` (`hasSavedSide`) | The Import button is disabled (greyed, `pointer-events:none`) unless a _valid_ saved payload exists for that side — key presence alone isn't enough (the payload is parsed and validated).                                                     |

## Results / download (Results)

| Feature                      | File                                                                       | What it does                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---------------------------- | -------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Output filesize bubble       | `Results.svelte` (`prettySize`)                                            | The panel footer shows the encoded output size in SI/base-1000 units (B…YB) at 3 significant figures (matching Squoosh's pretty-bytes), or "…" while encoding.                                                                                                                                                                                                                                                              |
| Percent-change badge + arrow | `Results.svelte` (`direction`, `percentMagnitude`)                         | A coloured badge with a directional triangle shows the size change vs the original — ↓ green-ward for smaller, ↑ for larger — hidden on the Original side and when change is 0/null.                                                                                                                                                                                                                                        |
| Per-side mirrored layout     | `Results.svelte`, `theme.css`                                              | Both sides use the same footer layout (stats left, download right), tinted by the side accent (coral left / azure right); the size-change badge is semantic (green = smaller, red = larger).                                                                                                                                                                                                                                |
| Download button              | `Results.svelte`, `OptionsPanel.svelte`                                    | The "Save" pill button is an `<a download>` pointing at the side's output object-URL; disabled (dimmed, no href) while encoding or before a result exists.                                                                                                                                                                                                                                                                  |
| Download filename            | `editor-session.svelte.ts` (`downloadName`)                                | Uses the original filename with the encoder's extension swapped in (e.g. `photo.webp`); the Original side downloads under the unchanged original name.                                                                                                                                                                                                                                                                      |
| Encoding spinner             | `Results.svelte`, `editor-session.svelte.ts` (`updateSpinner`)             | A spinner overlays the download button while a side is encoding, but only after a 500ms delay so fast encodes don't flash it.                                                                                                                                                                                                                                                                                               |
| In-progress badge (pill)     | `output/ProcessingBadge.svelte`, `editor-session.svelte.ts` (`activities`) | A 500ms-gated pill floats over each working side, then morphs into a green "done" dot. Wording reflects what changed this pass: first pass "Optimizing…"→"Optimized"; a real resize-control edit "Resizing…"→"Resized"; any other re-encode "Re-optimizing…"→"Re-optimized". The resize-vs-other call diffs each side's _effective_ resize recipe (resize counts only when target dims ≠ source) against the previous pass. |
| Redundant-pass skip          | `editor-session.svelte.ts` (`encodeSide`, `displayedSig`)                    | A side re-encodes only when its effective request (format, options, preprocessor, quantize, and resize **when it changes the size**) differs from what's on screen (`displayedSig`) **and** isn't already in the shared result cache. So enabling Resize at 100%, or toggling Premultiply/Linear RGB at 100%, is a true no-op — no encode, no pill — and any previously-seen request is served from cache instead of re-encoding. `processImage` independently skips the resampling call when target dims equal the source.                 |
| Type label (mobile)          | `Results.svelte`                                                           | On ≤760px the short format label (e.g. "WebP") is shown next to the size.                                                                                                                                                                                                                                                                                                                                                   |
| SVG gzip size line           | `Results.svelte`, `compress.ts` (`svg`), `src/lib/svg/optimize.ts`         | On the SVG lane the stats add a second line "gzip: <n> · was <n>" — the optimized text's gzip size vs the source's — because gzip is what an SVG actually costs over the wire and the raw-byte figure understates the win. The raw output never regresses: `keepOriginalSvg` reverts to the source text (0% change) if the optimized bytes aren't smaller.                                                                       |

## Encoder option panels

| Encoder        | File                            | Controls                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------------- | ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WebP           | `options/WebpOptions.svelte`    | Lossless toggle; lossy: Quality (0–100), Effort (0–6), and an Advanced reveal ("Preserve transparent data", Compress alpha, Alpha quality/filter, Auto-adjust filter strength→Filter strength, Strong filter, Filter sharpness [inverted], Sharp RGB→YUV, Passes, Spatial noise shaping, Preprocess select, Segments, Partitions); lossless: Effort (0–9 preset table), "Slight loss" [inverted near_lossless], "Discrete tone image" hint, "Preserve transparent data" (shown directly — lossless has no Advanced fold). |
| AVIF           | `options/AvifOptions.svelte`    | Lossless toggle (inferred from quality/subsample); Quality (0–99), Effort (0–10, inverse of speed), Advanced reveal (Subsample chroma→Sharp YUV when 4:2:0, Separate alpha quality, Extra chroma compression, Sharpness, Noise synthesis, Tuning [Auto/PSNR/SSIM], Log2 tile rows/cols).                                                                                                                                                                                                                                  |
| JPEG XL (jxl)  | `options/JxlOptions.svelte`     | Lossless transcode (JPEG sources only; disabled while a pixel step is active; hides every control below it when on); Lossless toggle (quality=100)→"Slight loss"; lossy: Quality (0–99); Effort (1–9); Advanced reveal (Alternative lossy mode [forced+disabled below quality 7], Auto edge filter→Edge preserving filter, Optimise for decoding speed, Noise-equivalent-to-ISO, Progressive rendering).                                                                                                                  |
| JPEG (mozJPEG) | `options/MozjpegOptions.svelte` | Quality (0–100); Advanced reveal (Channels [Grayscale/RGB/YCbCr]→[Auto subsample chroma, Subsample-by, Separate chroma quality], Pointless spec compliance↔Progressive/Optimize Huffman, Smoothing, Quantization table [9 options], Trellis multipass→Optimize zero block runs, Optimize after trellis, Trellis passes).                                                                                                                                                                                                  |
| PNG (oxiPNG)   | `options/OxipngOptions.svelte`  | Effort (level 0–6); Interlace toggle.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| SVG (SVGO)     | `options/SvgOptions.svelte`     | Vector-source lane (not a raster encoder). **Mode** select (Auto / Manual). Auto tries several settings behind a multi-scale visual gate and keeps the smallest that renders identically, showing a winner line (e.g. "Auto: precision 2 · reused paths"). Manual exposes **Precision** (0–4) plus an Advanced reveal: Multipass, Keep title & description, Reuse identical paths, Convert styles to attributes, Remove off-canvas paths, Remove width/height.                                                                  |

## Shared input components

| Component    | File                      | What it does                                                                                                                                                                                                         |
| ------------ | ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Range slider | `options/Range.svelte`    | Transparent native range over a styled track/thumb, with a value bubble that fades in only while actively dragging (pointerdown→pointerup), plus a mirroring underlined number input; values ≥10000 display as "Nk". |
| Select       | `options/Select.svelte`   | Styled native `<select>` with a custom arrow; `large` variant used for the encoder picker.                                                                                                                           |
| Toggle       | `options/Toggle.svelte`   | Switch-style checkbox (sliding thumb) used for section enablers (Resize/Film grain/Reduce palette).                                                                                                                             |
| Checkbox     | `options/Checkbox.svelte` | Icon-rendered box checkbox with a focus ripple, used for boolean encoder options.                                                                                                                                    |
| Revealer     | `options/Revealer.svelte` | Rotating-arrow checkbox used to expand/collapse "Advanced settings" sections.                                                                                                                                        |

## Snackbar (toast)

| Feature                | File                                          | What it does                                                                                                                                                              |
| ---------------------- | --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Toast notifications    | `snackbar-store.svelte.ts`, `Snackbar.svelte` | Bottom-centre messages that fly in/out; used for copy/save/import confirmations, Undo prompts, and the non-image rejection.                                               |
| Action buttons / Undo  | `snackbar-store.svelte.ts`, `Snackbar.svelte` | `show()` returns a promise resolving with the clicked action (e.g. `'undo'`), `'timeout'` (auto-dismiss), or `'dismiss'` (superseded); copy/import use this to wire Undo. |
| Auto-dismiss timing    | `snackbar-store.svelte.ts`                    | Defaults to 5s when actions are present, 4s otherwise; callers override (save uses 1.5s, error messages 3s).                                                              |
| Single-snack supersede | `snackbar-store.svelte.ts`                    | Showing a new snack while one is in flight resolves the old one as `'dismiss'` first.                                                                                     |

## Encoding pipeline & lifecycle

| Feature                  | File                                                                 | What it does                                                                                                                                                                                                                                                          |
| ------------------------ | -------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Per-side reactive encode | `src/routes/+page.svelte`, `editor-session.svelte.ts` (`encodeSide`) | An `$effect` per side re-encodes whenever its format/options/processor/preprocessor state changes, running decode→preprocess→resize→quantize→compress through the shared image pipeline (WASM codec worker) — unless the result is already on screen or in the cache (below).                                                          |
| Decode-back preview      | `compress.ts` (`compressFile`)                                       | The encoded output is decoded back to pixels for the "after" canvas so the preview shows the real codec result (artifacts included), aligned in size with the processed source.                                                                                       |
| Debounced re-encode      | `editor-session.svelte.ts` (`IMAGE_UPDATE_DELAY`)                    | Edits debounce by 100ms before re-encoding (so dragging a slider doesn't spawn an encode per frame); a fresh file encodes immediately. A cache hit skips the debounce entirely (it's shown synchronously).                                                                                                                                |
| Result cache (instant revisit) | `src/lib/result-cache.ts` (`ResultCache`), `editor-session.svelte.ts` (`encodeSide`) | Finished `CompressOutcome`s are cached keyed by their full encode-input signature, **shared across both sides** (a result is purely a function of its inputs, not which side shows it). Revisiting a recipe — via Undo/Redo, a re-selected setting, or the other side matching — shows it instantly with no re-encode. An LRU evicts by a pixel-byte budget (default 256 MiB) + entry cap (24), keeping currently-displayed results pinned. |
| Abort in-flight encodes  | `editor-session.svelte.ts`                                           | Each encode uses an `AbortController`; a superseding change aborts the prior run and revokes its orphaned object-URL (it is not cached).                                                                                                                                                 |
| Object-URL lifecycle     | `result-cache.ts` (`ResultCache`), `editor-session.svelte.ts`       | Each cached result owns its download object-URL; the cache revokes it on LRU eviction and on `clear()` (new file, `clearFile`, dispose). Aborted/duplicate encodes revoke their own URL inline. Displayed results stay pinned so their URL is never revoked underfoot.                                                                                                                                                                   |
| Undo/redo history        | `src/lib/editor/editor-history.svelte.ts` (`EditorHistory`), `editor-session.svelte.ts` (`watchHistory`/`undo`/`redo`) | A debounced `$effect` watcher (`HISTORY_COMMIT_DELAY` 350ms) commits a snapshot of the document (`{ sides, preprocessorState }`) once edits settle, so a slider drag coalesces into one step. Snapshots are stored in `$state.raw` and de-duped by an output-affecting signature; a guard flag + that signature stop a restore from re-committing. Undo/redo restore a snapshot, which re-runs the encode effect into a cache hit. History resets per image (`resetHistory` on load) and clears on `clearFile`. |
| Per-side error pill      | `src/routes/+page.svelte`, `editor-session.svelte.ts` (`firstError`) | The first side error message is shown as a status pill at the top-centre of the editor.                                                                                                                                                                               |
| Settings persistence     | `editor-session.svelte.ts` (`persistSettings`)                       | Both sides' format + per-format options are saved to localStorage (`app:settings:v3`) on change and rehydrated on next load (resize/quantize processor state is NOT part of this global persistence — only the per-side Save/Import buttons store processor state). |
| Resize-dimension seeding | `editor-session.svelte.ts` (`seedResizeDimensions`)                  | Once the first result lands, the Resize width/height inputs are seeded with the preprocessed source dimensions on sides where resize is disabled.                                                                                                                     |
| SVG/vector handling      | `editor-session.svelte.ts` (`pickFiles`/`isVectorSource`/`encodeSide`), `src/lib/svg/optimize.ts` | An SVG source defaults side 1 to the `svg` optimize lane and each side's resize method to "vector" (the "Vector" method is only offered then) so re-rasterising stays crisp. When a side's format is `svg`, `encodeSide` dispatches to the lazy-imported `optimizeSvgSide` (SVGO + fflate stay out of the main bundle) instead of the raster pipeline; if the current source isn't SVG the `svg` format falls back to `webP`. |

## Notable / hidden / non-obvious behaviors

- **Keyboard split presets (1/2/3)** in `two-up.ts` jump the comparison slider to start/middle/centre but are undocumented in the UI and suppressed while typing in an input.
- **Wheel always retargets to the left pinch-zoom**, so scroll-to-zoom works even with the cursor over the drag handle (`Output.svelte` event retargeting).
- **Safari-specific paths**: the smoothing/pixelated row is omitted from the View options popover on Safari (`image-rendering: pixelated` is a no-op in WebKit), so on Safari the popover shows only the Background row (`Output.svelte`); `pointerdown` is excluded from the retargeted event list on Safari; and `pinch-zoom.ts` sets `avoidPointerEvents: isSafari` to work around an iOS WebKit pointer-capture bug (#220196).
- **Android keyboard blur**: on `touchend` over the output, the active element is blurred so Android Chrome doesn't re-pop the software keyboard (ported `shouldBlurActiveElementAfterOutputRetarget`).
- **Firefox focus workaround**: the editable zoom input forces a style recalc (`getComputedStyle().transform`) before `focus()`/`select()` so Firefox doesn't drop focus on the just-inserted input (`focusOnMount`).
- **Quantize "ZX" konami easter-egg is intentionally OMITTED** from the Svelte port — `QuantizeOptions.svelte` notes the original's hidden konami-code palette type was dropped.
- **Trackpad pinch vs wheel**: `pinch-zoom.ts` distinguishes trackpad pinch (`ctrlKey`) from a plain wheel by using a tighter zoom divisor, and scales "lines"-mode deltas ×15.
- **Inverted-value sliders**: several "Effort"/"Slight loss"/"Filter sharpness" sliders display the inverse of the stored codec value (e.g. WebP near_lossless, filter_sharpness; AVIF effort=10−speed), so the displayed scale is friendlier than the raw option.
- **Lossless inference**: AVIF/JXL panels infer the Lossless checkbox state from the underlying quality/subsample rather than a dedicated stored flag, and JXL force-enables "Alternative lossy mode" (disabled) below quality 7.
- **Source-dependent control**: JXL's "Lossless transcode" is the only encoder option whose visibility depends on the loaded file rather than on other options. It appears for `image/jpeg` sources and routes to a separate wasm entrypoint that takes the file bytes instead of pixels, so it is mutually exclusive with rotate, resize, film grain and reduce-palette.
- **Save vs global persistence asymmetry**: the global auto-persist (`app:settings:v3`) stores only format + options (not processor/resize/quantize state), whereas the per-side Save button (`app:side-settings:*`) stores the full state including processor state — so a reload restores encoder choices but not resize/quantize toggles.
- **Import validation gating**: the Import button's enabled state and the import action share one `parseSavedSide` validator, so a corrupt or partial saved payload both disables the button and (if forced) shows an "invalid" snackbar — they can't drift.
- **`legacy-clip-compat` mode**: `<two-up>` positions its children absolutely and uses `clip-path` insets (rather than transform-based clipping) for cross-browser compatibility.
- **Result cache is shared across sides**: the cache key is the encode-input signature with **no side index**, because an encode is a pure function of its inputs — so a result the right side computed loads instantly if the left side later matches it (verified: ~20ms cached return vs ~470ms re-encode). Undo/redo and toggling a setting back are the same mechanism: a cache hit, not a re-encode.
- **Undo history excludes no-op state**: the history signature uses the active format's options and folds a disabled resize to `null`, so seeding the (hidden) resize dimensions after the first encode doesn't spawn a phantom undo step — mirroring the encode-skip logic.
- **Identity/Original download is the raw file**: the Original side's download is `URL.createObjectURL(file)` of the untouched original (0% change), not a re-encode.
- **No-JS / SSR safety**: `localStorage` access is guarded (`canUseLocalStorage`) so private-browsing/SSR don't throw; settings simply don't persist there.
